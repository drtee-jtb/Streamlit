services:
  - type: web
    name: streamlit-sales-forecast
    env: python
    branch: main
    buildCommand: .venv/bin/python -m pip install -r requirements.txt
    startCommand: .venv/bin/python -m streamlit run src/app.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
